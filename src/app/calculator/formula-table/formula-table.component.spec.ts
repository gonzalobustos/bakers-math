import { FormulaTableComponent } from './formula-table.component';

describe('FormulaTableComponent', () => {
  let component: FormulaTableComponent;

  beforeEach(() => {
    component = new FormulaTableComponent();
  });

  it('is created', () => {
    expect(component).toBeTruthy();
  });
});
